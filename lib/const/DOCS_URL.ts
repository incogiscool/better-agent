// The docs live in the standalone `packages/docs` Nextra app, deployed to its
// own subdomain. Centralised here so links across the marketing site point to
// one place.
export const DOCS_URL = "https://docs.betteragent.dev";
